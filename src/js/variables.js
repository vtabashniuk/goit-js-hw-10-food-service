const themeSwitcher = document.querySelector('#theme-switch-toggle');
const bodyElement = document.querySelector('body');
const Theme = {
  LIGHT: 'light-theme',
  DARK: 'dark-theme',
};

export { themeSwitcher, bodyElement, Theme };
